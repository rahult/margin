node_modules/
dist/
dist-ssr/
*.local
.DS_Store
src-tauri/target/
src-tauri/gen/
storybook-static/
*.tsbuildinfo
*.log

# Env — never commit credentials
.env
.env.*
!.env.example
!.env.sample

# Local knowledge store (user data)
server/data/

# Backend runtime deps (installed per platform in CI)
server/node_modules/
src-tauri/binaries/
